#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash; -*-
#MISE description="Start local Worker dev against the remote production Activate RI D1 database"
#MISE raw=true

set -euo pipefail

npx wrangler dev --env production-data --port "${PORT:-8787}" "$@"
