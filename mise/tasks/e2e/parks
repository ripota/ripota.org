#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash; -*-
#MISE description="Verify park geometry and public event flows on desktop and mobile"
#MISE raw=true

set -euo pipefail

npx playwright test --config=playwright.parks.config.ts "$@"
