---
import "../styles/global.css";
import { defaultShareImage, siteIdentity } from "../data/site";

type Props = {
  title?: string;
  description?: string;
  canonicalPath?: string;
  privatePage?: boolean;
  image?: {
    src: string;
    alt: string;
    width?: number;
    height?: number;
    type?: string;
  };
};

const {
  title = siteIdentity.name,
  description = siteIdentity.tagline,
  canonicalPath = "/",
  privatePage = false,
  image = defaultShareImage,
} = Astro.props;
const pageTitle = title === siteIdentity.name ? title : `${title} | ${siteIdentity.name}`;
const canonicalUrl = new URL(canonicalPath, siteIdentity.url).toString();
const socialImageBaseUrl = import.meta.env.DEV ? Astro.url.origin : siteIdentity.url;
const imageUrl = new URL(image.src, socialImageBaseUrl).toString();
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    {!privatePage && <meta name="description" content={description} />}
    {!privatePage && <link rel="canonical" href={canonicalUrl} />}
    <link rel="icon" type="image/svg+xml" href="/assets/logos/ri-pota-coastal-signal.svg" />
    <link rel="apple-touch-icon" href="/assets/logos/ri-pota-coastal-signal.png" />
    {!privatePage && <meta property="og:site_name" content={siteIdentity.name} />}
    {!privatePage && <meta property="og:type" content="website" />}
    {!privatePage && <meta property="og:title" content={pageTitle} />}
    {!privatePage && <meta property="og:description" content={description} />}
    {!privatePage && <meta property="og:url" content={canonicalUrl} />}
    {!privatePage && <meta property="og:image" content={imageUrl} />}
    {!privatePage && <meta property="og:image:alt" content={image.alt} />}
    {!privatePage && image.width && <meta property="og:image:width" content={String(image.width)} />}
    {!privatePage && image.height && <meta property="og:image:height" content={String(image.height)} />}
    {!privatePage && image.type && <meta property="og:image:type" content={image.type} />}
    {!privatePage && <meta name="twitter:card" content="summary_large_image" />}
    {!privatePage && <meta name="twitter:title" content={pageTitle} />}
    {!privatePage && <meta name="twitter:description" content={description} />}
    {!privatePage && <meta name="twitter:image" content={imageUrl} />}
    {!privatePage && <meta name="twitter:image:alt" content={image.alt} />}
    <meta name="theme-color" content="#0f766e" />
    <title>{pageTitle}</title>
  </head>
  <body>
    <a class="skip-link" href="#main">Skip to content</a>
    <slot />
  </body>
</html>
